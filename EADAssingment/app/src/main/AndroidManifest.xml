<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EADApp"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <activity android:name="com.example.eadapp.activity.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="com.example.eadapp.activity.RegisterActivity" />
        <activity android:name="com.example.eadapp.activity.MainActivity" />
        <activity android:name="com.example.eadapp.activity.CartActivity" />
        <activity android:name="com.example.eadapp.activity.OrderTrackingActivity" />
        <activity android:name="com.example.eadapp.activity.ProductDetailActivity" />
        <activity android:name="com.example.eadapp.activity.VendorFeedbackActivity" />


    </application>

    <uses-permission android:name="android.permission.INTERNET"/>

</manifest>
